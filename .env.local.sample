# Local development environment configuration
# Copy this file to .env.local and update with your actual values

# Polygon API - Required for market data
POLYGON_API_KEY=your_polygon_key_here

# Redis - Local development
REDIS_URL=redis://localhost:6379/0

# Discovery thresholds - Tune for testing
AMC_PRICE_CAP=100
AMC_MIN_DOLLAR_VOL=20000000
AMC_COMPRESSION_PCTL_MAX=0.15
AMC_MAX_CANDIDATES=15
AMC_COMPRESSION_LOOKBACK=60
AMC_EXCLUDE_FUNDS=true
AMC_EXCLUDE_ADRS=true

# Squeeze detection factor weights (renormalize to available factors)
AMC_W_VOLUME=0.25
AMC_W_SHORT=0.20
AMC_W_CATALYST=0.20
AMC_W_SENT=0.15
AMC_W_OPTIONS=0.10
AMC_W_TECH=0.10

# Squeeze detection thresholds
AMC_REL_VOL_MIN=3
AMC_ATR_PCT_MIN=0.04
AMC_FLOAT_MAX=50000000
AMC_BIG_FLOAT_MIN=150000000
AMC_SI_MIN=0.20
AMC_BORROW_MIN=0.20
AMC_UTIL_MIN=0.85
AMC_IV_PCTL_MIN=0.80
AMC_PCR_MIN=2.0

# Intraday features (1 to enable minute data)
AMC_INTRADAY=0

# ATR-based targets and risk management
AMC_R_MULT=2.0
AMC_ATR_STOP_MULT=1.5
AMC_MIN_STOP_PCT=0.02

# Optional: Custom universe for testing (comma-separated)
# AMC_DISCOVERY_UNIVERSE=AAPL,MSFT,NVDA,AMZN,GOOGL,META,TSLA,AMD,AVGO,PLTR

# Database (if testing full API)
# DATABASE_URL=postgresql://username:password@localhost:5432/amc_trader

# Copy to .env.local for local runs:
# cp .env.local.sample .env.local